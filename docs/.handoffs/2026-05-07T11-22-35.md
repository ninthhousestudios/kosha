# kosha — handoff

Date: 2026-05-07

## Completed this session

- kosha/11 (b584f56): MCP query tools — kosha_leaf, kosha_leaves, kosha_outline
- kosha/12+13 (5b418d9): PDF decomposer + multimodal image embedding

## Pick up next

### 1. Test kosha_search on PDF content

PDF data is in the DB (two leaves: text PDF + mixed scanned PDF). Verify vector search returns sensible results, especially for image-embedded chunks (multimodal retrieval via shared embedding space). Start the MCP server and query.

### 2. Format decomposers — epub, markdown/HTML (kosha/14, kosha/15)

Both fan out from the tracer bullet. The Decomposer trait now takes `&[u8]` and returns `SegmentContent::Text` or `SegmentContent::Image`. File extension dispatch is in `ingest.rs::decomposer_for()`.

### 3. Hybrid search (kosha/22)

Lexical leg + RRF fusion. Currently search is vector-only.

### 4. HttpEmbedder image support

The `embed_image_bytes` method returns "not supported" for the HTTP provider. If the target API (e.g. ollama) supports multimodal embedding input, wire it up. Low priority — local embedder works.

## Task graph

PRD is kosha/7. Done: 8, 9, 10, 11, 12, 13, 20, 23. Remaining: 14 (epub), 15 (md/html), 16 (kosha_ingest MCP tool), 17 (directory ingest), 18 (systemd unit), 21 (LLM cache), 22 (hybrid search), 24 (explain mode), 25 (graceful tiering doc).

## Context the next session needs

- Decomposer trait is now `fn decompose(&self, content: &[u8], source_path: &str) -> Result<Vec<Segment>>` with `SegmentContent` enum
- LocalEmbedder uses `Qwen3VLEmbedding` (not `Qwen3TextEmbedding`) — loads vision tower, supports both text and image embedding
- PDF rendering uses poppler-rs + cairo-rs at 150 DPI
- DB has test data: 2 leaves (text PDF 18 pages, mixed PDF 2 pages with 1 scanned)
- `DATABASE_URL=postgres:///kosha` — no .env file, pass on command line
