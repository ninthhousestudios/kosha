# kosha — handoff

Date: 2026-05-06

## Pick up next

### 1. Wire gemm-bf16 → candle → fastembed (kosha/8, HITL)

The critical prerequisite. Point candle's gemm dependency at `~/soft/gemm` (branch `bf16`), enable the bf16 feature, simplify the matmul code to use mixed-precision packing. Then verify fastembed runs Qwen3-VL-Embedding-2B in BF16 with 8x relevant/irrelevant quality ratio matching the PyTorch baseline.

Local repos: `~/soft/gemm` (bf16 branch), `~/soft/candle` (bf16-cpu-matmul branch), `~/soft/fastembed-local` (dtype patches).

### 2. Project scaffold + schema (kosha/9, AFK)

Can start in parallel with kosha/8. Rust workspace, Cargo.toml, Postgres schema migration (leaves/segments/chunks + pgvector), CLI skeleton (`kosha serve`, `kosha ingest`), `kosha_health` MCP tool, systemd unit.

### 3. Tracer bullet (kosha/10, AFK)

Blocked by 8 + 9. End-to-end: plain text file → BLAKE3 hash → decompose → chunk (512 tokens, zero overlap) → embed → Postgres → kosha_search + kosha_read. This builds every core module.

## Task graph

PRD is `kosha/7`. Slices are `kosha/8` through `kosha/18`. Critical path: 8 → 10 → 12 → 13 → 16. Format decomposers (12, 14, 15) fan out in parallel after the tracer bullet.

## Context the next session needs

- `CONTEXT.md` has the domain glossary (leaf, segment, chunk, citation)
- `docs/architecture.md` is STALE — still says "book" and has old two-table schema. Needs rewriting to match decisions from this session. All decisions are stored in chitta.
- `docs/kosha-sketch.md` and `docs/model-server-sketch.md` are superseded by the PRD but kept as historical context.

## Blockers

- gemm-bf16 → candle → fastembed wiring (kosha/8) blocks all embedding work
- Both upstream PRs (candle #3503, gemm #40) waiting on maintainers — but local MIT forks work

---

## 2026-05-07 — qi comparison session (append)

Reviewed qi (`/home/josh/soft/qi`, Go single-binary RAG CLI) and lifted six ideas into kosha as needs-triage tasks:

- **kosha/20** — adopt qi-style break-point chunker. Bounded variable size (target ± tolerance, hard ceiling), overlap=0, segment edges as hard cuts. Requires updating `CONTEXT.md:16` glossary line ("uniform-sized" → bounded-variable, structurally-coherent). Decision in chitta: `kosha-chunker-bounded-variable-2026-05-07`.
- **kosha/21** — LLM response cache (parked until kosha_ask exists).
- **kosha/22** — hybrid search (lexical leg + RRF fusion + strong-lexical short-circuit).
- **kosha/23** — provider abstraction for embedding/generation. **Highest-leverage near-term task**: every additional caller of fastembed/candle without a trait wrapping makes the eventual swap (multimodal model, remote provider) more invasive. Blocks kosha/11 (more MCP tools).
- **kosha/24** — `--explain` mode (MCP-first). Depends on kosha/22.
- **kosha/25** — graceful-tiering doc. Relates to kosha/22.

Edges: kosha/14 (epub) and kosha/15 (md/html) relate_to kosha/20 — landing those decomposers before the new chunker means re-chunking content later.

Reference repo stays at `/home/josh/soft/qi`.
